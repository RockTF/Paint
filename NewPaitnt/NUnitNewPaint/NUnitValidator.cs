using NUnit.Framework;
using NewPaitnt;
using Xunit;

namespace NUnitNewPaint
{
    public class NUnitValidator
    {
        

        //[NUnit.Framework.Theory]
        //[InlineData("Ksy")]
        //public void InputValidation_CheckingTheInputOfTheFirstAndLastName(string exected, string name)
        //{
        //    var validator = new Validator();

        //    string result = validator.InputValidation(name);

        //    result.Should().Be(expected);
        //}
    }
}
